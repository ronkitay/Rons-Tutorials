# Important

To execute anything in this directory you need to run it this way:

```shell
PYTHONPATH=$(pwd) python3 <relative-path-to-script-name>
```

If you do not - Python will not find dependencies properly
