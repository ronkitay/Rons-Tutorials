val value1 = 1
val value2 = 1.0F
val value3 = 1.0

val total: Double = value1 + value2 + value3 

println("Total = $total")

val prefix = "Before"
val suffix = "After"

val fullText = prefix + " and " + suffix
println(fullText)
val fullTextV2 = "$prefix and $suffix - V2"
println(fullTextV2)

val isItGood = true
println(isItGood)
